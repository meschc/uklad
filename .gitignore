node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
logs
*.log
npm-debug.log*

# Настоящие переменные окружения. Образец (`.env.example`) — наоборот, в
# репозитории: по нему видно, что нужно заполнить, и он заполнен заглушками.
.env
.env.*
!.env.example

# Сборочный мусор
coverage
tsconfig.tsbuildinfo
.claude

# Артефакты сквозных тестов: трассы, скриншоты, отчёт
test-results
playwright-report
blob-report
.playwright

# Референсы дизайна — картинки для работы, не часть прототипа
refs

# Черновики сессий планирования: аудиты, планы, разбор конкурента.
# Внутренняя кухня, в репозитории ей делать нечего. Правило — на папку, а не
# на имена: список файлов приходилось дописывать после каждой сессии, и
# очередной черновик успевал попасть в коммит раньше, чем в этот файл.
docs/drafts/
