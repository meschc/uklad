# Перед отправкой в общий репозиторий — проверка типов и тесты. Линтер и
# форматтер здесь не нужны: их уже прогнал `pre-commit` по изменённым файлам.
#
# Тридцать секунд ожидания дешевле, чем красная сборка в CI и коммит-заплатка
# следом за ней. Если проверку нужно обойти осознанно — `git push --no-verify`.
npm run typecheck
npm test
