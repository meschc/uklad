# Перед коммитом — только то, что попало в индекс: форматтер и линтер по
# изменённым файлам. Секунда работы вместо полной проверки проекта; тяжёлое
# (типы, тесты, сборка) стоит на `pre-push` и в CI.
#
# Правила, по которым это работает, лежат в `lint-staged.config.js`.
npx lint-staged
