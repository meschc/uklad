/**
 * Модель данных «Уклада», разложенная по доменам. Импортировать всё равно
 * нужно из `@/lib/types` — этот файл собирает домены в одну точку, поэтому
 * перенос типа между файлами не ломает вызовы.
 *
 * - `plan`        — этаж, модули, полки, ряды, адрес места хранения (разд. 2.1–2.3);
 * - `warehouse`   — паспорт склада, персонал, партнёры, переписка;
 * - `catalog`     — товар, категории и их доп.поля (разд. 2.5, 3.8);
 * - `labels`      — шаблоны наклеек на тару (п.6);
 * - `fulfillment` — приёмка, коробки, паллеты, заявки, рейсы;
 * - `account`     — пользователь, сессия, профиль, экраны (разд. 3.1–3.4).
 *
 * Домены знают друг о друге ровно по делу: склад ссылается на этажи, заявки —
 * на адрес ячейки. Обратных ссылок нет — план ничего не знает о фулфилменте.
 */

export * from "./plan";
export * from "./warehouse";
export * from "./catalog";
export * from "./labels";
export * from "./fulfillment";
export * from "./account";
