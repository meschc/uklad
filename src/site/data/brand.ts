import { c } from "../lib/copy";

/**
 * Название марки. На английской версии оно записано латиницей не ради красоты:
 * в подвале, в шапке и в заголовке вкладки уже стоит «Uklad», и кириллический
 * знак рядом с ними читался бы как второе, другое имя того же сервиса.
 *
 * Лежит в данных, а не рядом со знаком в `components/Logo`. Имя нужно не только
 * знаку: его печатают заголовки вкладок (`lib/pageMeta`), разметка Schema.org
 * (`lib/jsonLd`) и подвал. Библиотеке незачем тянуть за собой svg на полсотни
 * строк ради одной строки текста.
 *
 * От `ORG.shortName` отличается назначением: там — как предпринимателя зовут в
 * документах, здесь — как продукт называется на витрине.
 */
export const BRAND = c("Уклад", "Uklad");
