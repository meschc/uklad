/**
 * Внешние координаты витрины. Вынесены отдельно, потому что меняются они не
 * по-продуктовому, а по-хозяйственному: появился адрес демо — правится одна
 * строка, а не три компонента.
 */
export const SITE = {
  /** Версия прототипа, о которой рассказывает витрина (см. RELEASES). */
  version: "0.3.0",
  /** Дата последней правки роадмапа, ISO. Показана в шапке первого экрана. */
  updatedAt: "2026-09-15",
  /**
   * Куда ведёт «Запустить у себя». Пока это раздел запуска в репозитории:
   * развёрнутого демо нет, и кнопка «Открыть демо» была бы обещанием, за
   * которым ничего не стоит. Появится адрес — меняется только эта строка.
   */
  demoUrl: "https://github.com/meschc/uklad#запуск",
  repoUrl: "https://github.com/meschc/uklad",
  /** Куда писать с предложением или вопросом по роадмапу. */
  issuesUrl: "https://github.com/meschc/uklad/issues",
} as const;
